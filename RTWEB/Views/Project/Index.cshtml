@* @model IEnumerable<Project> *@
@model HMSYSTEM.ViewModels.PagedResult<Project>
@using RTWEB.Enum
@{
    ViewData["Title"] = "Project";
}


<div class="container-fluid mt-3">
    <div class="d-flex justify-content-sm-between mb-2">
        <div>
            <input type="text" id="searchBox" class="form-control" placeholder="Search Project" />
        </div>

        <div class="">
            <a asp-controller="Project" asp-action="Save" class="btn btn-primary">Add Project</a>
        </div>
        
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="card mb-4">

                <!-- /.card-header -->
                <div class="card-body">
                    <table class="table table-bordered table-hover">
                        <thead>
                            <tr>
                                <th style="width: 10px">Sl</th>
                                <th>Name</th>
                                <th>Update Brnach</th>
                                <th>Last Update Date</th>
                                
                                <th style="width:20px;text-align:center">Action</th>
                            </tr>
                        </thead>
                        <tbody id="projectBoady">
                            @{
                                int count = ((Model.CurrentPage - 1) * Model.PageSize) + 1;
                            }
                            @foreach (Project item in Model.Items)
                            {
                                <tr>
                                    <td>@count</td>
                                    <td>@item.ProjectName</td>
                                    <td>@(item.UpdateBranch ?? "N/A")</td>
                                    <td>@(item.LastUpdateDate?.ToString("dd-MM-yyyy") ?? "N/A")</td>
                                    <td>
                                        <div class="d-flex justify-content-center btn-group-sm ">

                                            <a class="btn btn-primary ms-2" asp-route-id="@item.Id" asp- controller="Appointment" asp-action="Edit">
                                                <i class="fa fa-edit"></i>
                                            </a>

                                            <a class="btn btn-danger ms-2"
                                               asp-route-id="@item.Id"
                                               asp-controller="Project" asp-action="Delete">
                                                <i class="fa fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                                count++;
                            }

                        </tbody>
                    </table>
                </div>
                <!-- /.card-body -->
                <div class="card-footer clearfix">
                     @await Html.PartialAsync("_Pagination", Model) 
                </div>
            </div>
            <!-- /.card -->
        </div>
    </div>
</div>


<script>
    $(document).ready(function () {
        $('#searchBox').on('keyup', function () {
            var name = $(this).val();

            $.ajax({
                url: '/Project/Search',
                data: {
                    name: name,
                },

                success: function (data) {
                    let rows = '';

                    data.forEach((item,index) => {
                        rows += `<tr>
                            <td>${index + 1}</td>
                            <td>${item.projectName}</td>
                            <td>${item.updateBrnach}</td>
                            <td>${item.updateDate}</td>
                             <td>
                                        <div class="d-flex justify-content-center btn-group-sm ">

                                            <a class="btn btn-primary ms-2" asp-route-id="item.Id" asp- controller="Medicine" asp-action="Edit">
                                                <i class="fa fa-edit"></i>
                                            </a>

                                            <a class="btn btn-danger ms-2" asp-route-id="item.Id" asp-controller="Medicine" asp-action="Delete">
                                                <i class="fa fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                        </tr>`;
                    });

                    $('#projectBoady').html(rows);
                }
            });
        });
    });
</script>